---
id: cost-to-them-over-the-term
label: "What it costs them over the term"
used_by: [can-they-carry-it]
over: [price-position]
over_status: provisional   # the only assembly every signal using it reads. Not yet confirmed.
defined: false   # nothing here is written yet. See the body.
---

# What it costs them over the term
## What it counts

Not written yet.

This entry exists because one signal references this id: [`can-they-carry-it`](../signals/can-they-carry-it.md). What it actually counts (your told price, spread over whatever term and repayment shape the offering has), which records it needs and when the number stops meaning anything are all still to be decided. The term and the shape of the repayments are not told fields yet, so today the sum cannot be done at all (a told gap, named not faked).

It counts within [`price-position`](../assemblies/price-position.md). That is provisional: it is the only assembly every signal using this count reads, so it was derived, not decided.

## What it cannot see

Not written yet.
